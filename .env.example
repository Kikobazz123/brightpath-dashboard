# Copy to .env.local and fill in. .env* is gitignored — never commit real values.

# ---- Database -------------------------------------------------------------
# Neon Postgres connection string (Project: brightpath).
# Get it from https://console.neon.tech or `neonctl connection-string`.
DATABASE_URL="postgresql://user:password@host.neon.tech/neondb?sslmode=require"

# ---- AI provider ----------------------------------------------------------
# The pipeline is provider-agnostic. Set AI_PROVIDER and the matching key.
# Leave everything unset to run in "stub" mode: the pipeline still works
# end-to-end and returns clearly-labelled placeholder output, so the app is
# demoable before any key exists.
#
#   gemini  — Google AI Studio, ~1500 req/day free, no card. Recommended default.
#   groq    — Groq, ~1000 req/day free, very fast. Good failover.
#   openrouter — one key, many free models. Good for swapping models quickly.
#   anthropic  — paid (~$1/$5 per 1M tokens on Haiku 4.5). Not free with a
#                Claude Code subscription; the API is billed separately.
#   stub    — no network calls at all.
AI_PROVIDER="stub"

# https://aistudio.google.com/apikey
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-3.6-flash"

# https://console.groq.com/keys
# Model names go stale fast — three of the defaults in this file were already
# retired within months of being written. If a provider answers 404
# model_not_found, list what your key can actually see and set the name here,
# which takes effect without a deploy:
#   curl -s https://api.groq.com/openai/v1/models -H "Authorization: Bearer $KEY"
#   curl -s https://openrouter.ai/api/v1/models        # public, no key needed
GROQ_API_KEY=""
GROQ_MODEL="openai/gpt-oss-120b"

# https://openrouter.ai/keys
OPENROUTER_API_KEY=""
OPENROUTER_MODEL="z-ai/glm-5.2:free"

# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=""
ANTHROPIC_MODEL="claude-haiku-4-5"

# ---- Failover ------------------------------------------------------------
# A second provider, tried automatically when the primary fails in a way a
# retry could fix: a 429 rate limit, an exhausted daily quota, or a 5xx.
# A 4xx is treated as our bug and is not retried against anyone.
#
# This matters more than it looks. Without it, one exhausted free-tier quota
# means every lead captured until it resets reads NEEDS_REVIEW with no draft —
# the assistant silently stops assisting, exactly when traffic is highest.
#
# Takes a COMMA-SEPARATED CHAIN, tried in order:
#   AI_FALLBACK_PROVIDER="groq,openrouter"
# giving gemini -> groq -> openrouter -> stub. A single value also works.
#
# Use DIFFERENT providers, not extra keys on one: the point is to survive that
# provider's quota, and two keys on one account share it. The primary, "stub",
# unknown names and duplicates are all dropped from the chain with a warning.
#
# Depth is worth it when several people may hit the same free tier in the same
# hour — a demo being run repeatedly by judges, say. One fallback survives one
# provider running dry; two survives the first fallback being hit by the same
# burst. All three below are free and on separate accounts:
#   gemini     https://aistudio.google.com/apikey
#   groq       https://console.groq.com/keys
#   openrouter https://openrouter.ai/keys
#
# Note the chain advances on ANY provider failure, not only rate limits — a
# retired model name returns a non-retryable 404, and a healthy second provider
# should still carry the request.
#
# Verify after setting: `pnpm verify:provider` calls every link directly, so a
# bad key fails there rather than on the day the primary goes down.
AI_FALLBACK_PROVIDER=""

# ---- Speed-to-lead --------------------------------------------------------
# Minutes allowed between a lead arriving and its first real sales action.
# Industry research puts the 5-minute mark as the point where conversion falls
# off a cliff; 15 is a realistic team SLA. Lower it to demo breach behaviour.
SLA_FIRST_TOUCH_MINUTES="15"

# ---- Mailbox --------------------------------------------------------------
# One Gmail account, over SMTP, used for everything the app sends:
#
#   - an alert to MAIL_INBOX when a website enquiry arrives, carrying the score
#   - an acknowledgement to whoever filled the form in
#   - the drafted follow-up, when a rep presses "Send now" on a lead
#   - replies typed into the Inbox reading pane
#
# GMAIL_APP_PASSWORD must be an APP PASSWORD, not the account password — Gmail
# rejects account passwords over SMTP. Create one at
# https://myaccount.google.com/apppasswords (2-Step Verification must be on),
# and paste the 16 characters with the spaces removed.
#
# Leave these empty and the app still runs end to end: leads are captured,
# scored and drafted exactly as before, and every send button either disappears
# or says why it cannot work. Nothing silently pretends to have sent.
#
# A transactional provider (Resend, Postmark) would deliver better at volume.
# Gmail is chosen here so that sent mail lands in the same Sent folder as
# everything typed by hand, and replies come back to the same inbox.
GMAIL_USER=""
GMAIL_APP_PASSWORD=""

# Display name on outgoing mail, and where enquiry alerts are delivered.
# MAIL_INBOX defaults to GMAIL_USER — set it only to notify a different address.
MAIL_FROM_NAME="Brightpath Solutions"
MAIL_INBOX=""

# Host and port. Defaults suit Gmail; 465 is implicit TLS, 587 uses STARTTLS.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="465"

# Used to build the "Open the lead" link inside enquiry alerts. Without it the
# alert names the lead id instead of linking to it. Vercel supplies its own
# production URL, so this is only needed for a custom domain.
NEXT_PUBLIC_APP_URL=""

# ---- Demo auth ------------------------------------------------------------
# The template ships sign-in pages but no auth backend. Until one is wired,
# requests carry this shared token and resolve to DEMO_TENANT_ID.
#
# Leaving DEMO_API_TOKEN empty opens the API to anyone, which is fine on a
# laptop. In production the app refuses every request instead, rather than
# letting a forgotten variable quietly publish the whole lead database.
DEMO_API_TOKEN="brightpath-demo-token"
DEMO_TENANT_ID="brightpath"

# Recorded as the actor on every activity row a dashboard button creates.
# "system" would be a lie for something a person clicked.
DEMO_ACTOR="rep"

# ---- Rate limits ----------------------------------------------------------
# Requests per minute per IP on the two routes that cannot require a token: the
# public capture form and the webhook receivers. Both run the AI pipeline
# inline, so an unauthenticated loop against them spends the day's model quota.
#
# Counters live in process memory, so on a serverless host each instance keeps
# its own and a cold start forgets them. That stops an accident, not an
# attacker — a real deployment moves the counter into Redis or Vercel KV.
CAPTURE_RATE_LIMIT="20"
WEBHOOK_RATE_LIMIT="60"
